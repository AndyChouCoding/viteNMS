{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions, plus execute rights scoped to only the bundled backend sidecar binary (least-privilege per the project's IEC 62443 design baseline — no general shell access)",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/open-vision-backend",
          "sidecar": true,
          "args": false
        }
      ]
    }
  ]
}
